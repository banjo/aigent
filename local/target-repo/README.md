# Aigent Local Target Repo

This directory is a small local target repo for testing `aigent run` behavior.

Run Aigent against this target repo from the repository root:

```bash
pnpm start:fixture
```
